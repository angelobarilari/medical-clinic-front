import Header from "../../components/Header"
import axios from "axios"

const Home = () => {
    /*
    const baseUrl = "http://localhost:3000/patients/all"

    axios.get(baseUrl)
            .then(res => {
                console.log(res.data)
            })
            .catch(err => {
                console.log(err)
            })*/

    return (
        <div>
            <Header />

            minha home
        </div>
    )
}

export default Home

